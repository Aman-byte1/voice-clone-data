# Core
torch>=2.0.0
torchaudio>=2.0.0
numpy
soundfile
pandas
tqdm
datasets<4.0.0
librosa

# Chatterbox Multilingual TTS
# Option A — from PyPI (if available):
#   pip install chatterbox-tts
# Option B — from GitHub (recommended for multilingual):
chatterbox-tts @ git+https://github.com/resemble-ai/chatterbox.git

# Watermarker dependency (may need patching on some systems)
resemble-perth
